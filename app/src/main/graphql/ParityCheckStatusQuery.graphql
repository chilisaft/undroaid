query ParityCheckStatusQuery {
    array {
        parityCheckStatus {
            status
            progress
            speed
            errors
            correcting
            paused
            running
        }
    }
}
