query NotificationsListQuery {
    notifications {
        list(filter: { type: UNREAD, offset: 0, limit: 50 }) {
            id
            title
            subject
            description
            importance
            formattedTimestamp
        }
    }
}
