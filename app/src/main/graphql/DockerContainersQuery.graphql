query DockerContainersQuery {
    docker {
        containers {
            id
            names
            image
            state
            status
            iconUrl
            webUiUrl
        }
    }
}
